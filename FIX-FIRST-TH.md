# วิธีอัปเดตไฟล์เว็บที่แก้แล้ว

ปัญหาที่พบ: ชุดอัปเดตถูกวางเป็นโฟลเดอร์ FACTORBOXES-* แยกไว้ ทำให้ app/lib ที่เว็บใช้จริงยังเป็นไฟล์เก่า และ TypeScript ตรวจไฟล์แพตช์ซ้ำด้วย
ชุดนี้รวมไฟล์ทั้งหมดในตำแหน่งที่ถูกต้องแล้ว พร้อมลบออเดอร์ เลข 0000 ซ่อนหลังบ้าน และตะกร้าลอย

## 1. ทำใน Supabase ก่อน

เปิด SQL Editor > New query
เปิดไฟล์ supabase/04-order-numbers.sql ในชุดนี้ คัดลอกทั้งหมดแล้ว Run
ถ้าเคยรันไฟล์นี้แล้ว สามารถรันซ้ำได้โดยไม่รีเซ็ตเลขออเดอร์
ไม่ต้องรัน 01-schema.sql, 02-products.sql หรือ 03-storage.sql ใหม่
ไม่ต้องลบข้อมูลหรือสร้างโปรเจกต์ Supabase ใหม่

## 2. อัปโหลด GitHub

แตก ZIP แล้วเปิดโฟลเดอร์ web-app-main
เข้า repository เดิมของคุณ (Factorboxes/web-app)
เลือก Add file > Upload files
อัปโหลดไฟล์และโฟลเดอร์ด้านใน web-app-main ไปที่หน้าแรก repository
อย่าอัปโหลดโฟลเดอร์ web-app-main ครอบอีกชั้น และอย่าอัปโหลด ZIP ทั้งก้อน
ให้เห็น app, lib, components, public และ package.json อยู่หน้าแรกเหมือนเดิม
แบ่งอัปโหลดทีละโฟลเดอร์ได้หากมีไฟล์มากเกินไป
อย่าลืมอัปโหลด tsconfig.json และ package.json ที่หน้าแรกด้วย
Commit changes แล้วรอ Vercel Deploy สำเร็จ

โฟลเดอร์เก่า FACTORBOXES-Delete-Test-Orders, FACTORBOXES-Order-Numbers-0000 และ FACTORBOXES-Floating-Cart ใน GitHub ลบได้หลังอัปโหลดสำเร็จ เพราะรวมเข้ากับ app/lib แล้ว
ชุดนี้ตั้งให้ TypeScript ข้ามโฟลเดอร์แพตช์เก่าแล้ว แม้ยังไม่ได้ลบออกจาก GitHub
ไม่ต้องเปลี่ยน Environment Variables ที่ใช้งานได้อยู่

## 3. ตรวจหน้าเว็บ

รีเฟรชเว็บหลัง Deploy
- ตะกร้าลอยมุมขวาล่าง เปิดตะกร้าเดียวกับร้าน
- ไม่มีลิงก์หลังบ้านบนหน้าร้าน ให้เข้า /admin โดยตรง
- ออเดอร์เรียง 0000 เป็นต้นไป และไม่ใช้เลขที่ลบไปแล้วซ้ำ
- เจ้าของลบออเดอร์ทดลองผ่านหน้าจัดการออเดอร์ได้

ถ้า Build ไม่ผ่าน ส่งข้อความ Error จาก Build Logs ของ Vercel มา
หากหน้าออเดอร์โหลดไม่ได้ ให้ตรวจว่ารัน 04-order-numbers.sql สำเร็จแล้ว

ชุดนี้ผ่าน npm ci, production build และการทดสอบ PostgreSQL ในเครื่องแล้ว
ยังไม่ได้ Deploy ไปบัญชี Vercel ของคุณ
