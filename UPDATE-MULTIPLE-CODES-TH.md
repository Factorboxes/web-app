# ชุดแก้ไขสร้าง CODE หลายรหัส
จากภาพ: column "starts_at" of relation "coupons" does not exist
สาเหตุที่แสดงคือฐานข้อมูลยังขาดคอลัมน์ ไม่ใช่ข้อจำกัดว่ามี CODE ได้เพียงหนึ่งรหัส

## 1. อัปเดต Supabase ก่อน (จำเป็น)
เปิด Supabase โปรเจกต์ที่เชื่อมกับเว็บไซต์ > SQL Editor > New query
คัดลอกเนื้อหาทั้งหมดจาก supabase/18-coupon-quotas.sql แล้วกด Run
ต้องรันสำเร็จทั้งไฟล์ หากมีข้อความผิดพลาด ให้ส่งข้อความนั้นมาตรวจสอบก่อนดำเนินการต่อ
ไฟล์นี้เพิ่มคอลัมน์และระบบนับสิทธิ์ ไม่ลบ CODE เดิม รันซ้ำได้
หากเคยรันแล้วแต่ยังพบข้อผิดพลาด ให้ตรวจว่าเป็นโปรเจกต์เดียวกับฐานข้อมูลที่ตั้งใน Vercel

## 2. อัปโหลดไฟล์ GitHub
แตก ZIP แล้วอัปโหลดโฟลเดอร์ app และ lib ทับตำแหน่งเดียวกับโปรเจกต์เดิม
เช่น app/admin/coupons.tsx และ app/api/coupons/route.ts อย่าสร้าง app/app
โฟลเดอร์ supabase และ tests เก็บใน repository ได้ แต่การอัปโหลด SQL ขึ้น GitHub ไม่ได้รัน SQL ให้
Commit changes แล้วรอ Vercel แสดง Ready จากนั้นรีเฟรชหน้า /admin

## 3. วิธีใช้
กด ＋ สร้าง CODE ใหม่ กรอกรหัสไม่ซ้ำ เช่น WELCOME50 และเงื่อนไข แล้วกดสร้าง CODE
เมื่อบันทึกสำเร็จ ช่องกรอกจะล้าง พร้อมสร้างรหัสถัดไป และรายการด้านล่างจะอัปเดต
แก้รหัสเดิมด้วยปุ่มแก้ไขของแถวนั้น ไม่ใช้ปุ่มสร้างใหม่
รหัสซ้ำจะถูกปฏิเสธ ไม่เขียนทับเงื่อนไขเดิม
หากบันทึกไม่สำเร็จ ช่องที่กรอกไว้จะยังอยู่ และแสดงข้อความสาเหตุ
สร้างได้หลายรหัสโดยเพิ่มทีละรหัส ไม่มีเพดานหนึ่งรหัส
ลูกค้ายังใช้ได้หนึ่ง CODE ต่อออเดอร์
จำนวนสิทธิ์ทั้งหมดเป็นจำนวนครั้งที่ใช้ CODE นั้น ไม่ใช่จำนวนรหัสที่จะสร้าง
การยกเลิกหรือลบออเดอร์ไม่คืนสิทธิ์อัตโนมัติ

## การตรวจสอบ
ทดสอบฐานข้อมูลจำลองจาก schema เดิมที่ขาด starts_at แล้วรัน SQL และสร้างสามรหัสต่อเนื่องสำเร็จ
ทดสอบรหัสซ้ำไม่ทับข้อมูล การแก้ไข การรักษา CODE หลังรัน SQL ซ้ำ สิทธิ์แอดมินและโควตา ผ่าน
Production build ผ่าน ยังไม่ได้อัปโหลดหรือตรวจฐานข้อมูลจริงของร้าน
