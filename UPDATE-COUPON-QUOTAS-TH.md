# อัปเดตระบบ CODE ส่วนลด

ชุดไฟล์เฉพาะระบบ CODE ไม่ใช่เว็บทั้งชุด

วิธีติดตั้ง
1. แตก factorboxes-coupon-upgrade.zip
2. เปิด supabase/18-coupon-quotas.sql คัดลอกทั้งหมดไป Supabase → SQL Editor → New query → Run ก่อนอัปโหลดโค้ดเว็บ รันซ้ำได้ ไม่ลบโค้ดหรือออเดอร์เดิม
3. ใน GitHub ที่ราก Repository (ตำแหน่ง package.json) รวมโฟลเดอร์ app, lib, supabase เข้าโฟลเดอร์เดิม กด Commit changes ห้ามให้เป็น app/app
4. รอ Vercel ขึ้น Ready รีเฟรชหลังบ้าน → โปรโมชั่น / CODE

ไฟล์เว็บที่เปลี่ยน:
app/admin/coupons.tsx
app/admin/coupons.css (ใหม่)
app/api/coupons/route.ts
app/api/shop/route.ts
lib/coupons.ts
lib/coupon-rules.ts (ใหม่)

ไฟล์ SQL ต้องรันเอง การอัปโหลดเข้า GitHub อย่างเดียวไม่อัปเดตฐานข้อมูล
หากไฟล์เดิมบน GitHub มีการปรับเพิ่มเองภายหลัง ให้รวมการแก้ไขแทนการทับส่วนนั้น

หน้าจอใหม่
- จำนวน CODE ทั้งหมด / ใช้งานได้ / จำนวนการใช้ / หมดอายุหรือสิทธิ์เต็ม
- ค้นหา CODE แก้ไขและเปิด/ปิดได้
- ส่วนลดเป็นบาทหรือเปอร์เซ็นต์ และยอดสินค้าขั้นต่ำหลังใช้เรทราคา
- วันที่เริ่ม: เริ่ม 00:00 น. เวลาไทย; ว่าง = ทันที
- วันหมดอายุ: ถึงสิ้นวันเวลาไทย; ว่าง = ไม่หมดอายุ
- จำนวนสิทธิ์ทั้งหมด: เช่น 100 คือรวมกันได้ 100 ออเดอร์; ว่าง = ไม่จำกัด
- ต่อสมาชิก: เช่น 1 คือบัญชีสมาชิกนี้ใช้ CODE นี้ได้ 1 ครั้งตลอดอายุ CODE; ว่าง = ไม่จำกัด
- ถ้าจำกัดต่อสมาชิก ลูกค้าต้องเข้าสู่ระบบ อิงบัญชีสมาชิก ไม่ใช่บุคคลจริงหรือเบอร์โทร
- 1 CODE ต่อออเดอร์ ไม่ซ้อนหลายโค้ด

ตัวอย่าง (ต้องสร้างเอง ไม่สร้างโปรโมชั่นโดยอัตโนมัติ): WELCOME50 ลด 50 บาท ยอดขั้นต่ำ 750 บาท จำนวนรวม 100 สิทธิ์ ต่อสมาชิก 1 ครั้ง

การนับสิทธิ์
- การกดลอง CODE ในตะกร้ายังไม่หักสิทธิ์ หักเมื่อออเดอร์ถูกบันทึกสำเร็จเท่านั้น
- ตรวจและจองสิทธิ์ในธุรกรรมฐานข้อมูลเดียวกัน ล็อกแถว CODE เพื่อไม่ให้หลายออเดอร์แย่งเกินโควตา
- ถ้าบันทึกออเดอร์ล้มเหลว สิทธิ์จะไม่ถูกหัก
- ออเดอร์ที่รอชำระก็นับสิทธิ์แล้ว การยกเลิก/ลบไม่คืนสิทธิ์อัตโนมัติ ประวัติการใช้ไม่ถูกลบตามออเดอร์
- SQL นับการใช้เดิมจากออเดอร์ที่ยังมีอยู่ รวมรายการที่ยกเลิกแล้ว หากเคยลบออเดอร์ไปก่อนติดตั้ง จะกู้จำนวนการใช้ของรายการที่ลบไปแล้วไม่ได้
- หากต้องการเริ่มโปรโมชั่นรอบใหม่ให้สร้างรหัสใหม่ โค้ดเดิมยังคงจำนวนใช้เดิมแม้แก้วันที่
- หากลดโควตาต่ำกว่าจำนวนใช้ไปแล้ว โค้ดจะสิทธิ์เต็ม ไม่ย้อนกลับไปเปลี่ยนออเดอร์เก่า

ตรวจหลังติดตั้ง
สร้าง CODE ทดลองจำกัดรวม 1 สิทธิ์ ต่อสมาชิก 1 ครั้ง ทดสอบยืนยันออเดอร์หนึ่งครั้ง แล้วตรวจ used/remaining ในหลังบ้าน และลองใช้ครั้งที่สองต้องถูกปฏิเสธ เมื่อทดสอบเสร็จปิด CODE ทดลอง (การลบออเดอร์ไม่คืนสิทธิ์ตามนโยบายข้างต้น)

ทดสอบ SQL การนับโควตา การย้อนธุรกรรม และ production build แล้ว ยังไม่ได้รัน SQL หรืออัปโหลดเว็บจริงให้
