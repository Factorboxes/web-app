# เพิ่มสถิติคนเข้าเว็บไซต์

## ขั้นตอนอัปเดต (ทำทั้งสองส่วน)
1. เปิด Supabase โครงการเดิม > SQL Editor > New query
2. เปิดไฟล์ supabase/05-analytics.sql คัดลอกทั้งหมดไปวาง แล้วกด Run เพียงครั้งเดียว (รันซ้ำได้ ไม่ลบสถิติเดิม)
3. แตก ZIP แล้วอัปโหลดไฟล์ด้านใน web-app-main ทับตำแหน่งเดิมใน GitHub Factorboxes/web-app ไม่อัปโหลดเป็นโฟลเดอร์ซ้อน
4. รอ Vercel Deploy สำเร็จ
5. เปิดหน้าร้านเพื่อเริ่มบันทึก แล้วเข้าหลังบ้าน /admin > สถิติคนเข้าเว็บ > รีเฟรชสถิติ

ผู้ติดตั้งใหม่ให้ทำตาม START-HERE-TH.md และรัน 05-analytics.sql เพิ่มหลัง 04-order-numbers.sql

## สิ่งที่ดูได้
- จำนวนเปิดหน้าเว็บและจำนวนผู้เข้าชมโดยประมาณ
- เลือกวัน สัปดาห์ เดือน ปี และวันที่อ้างอิง ตามเวลาไทย
- ตารางสถิติรายวันและหน้าที่เปิดดูมากที่สุด
- เจ้าของร้านและแอดมินที่มีสิทธิ์ “ดูสรุปยอดขายและสถิติคนเข้าเว็บ” เข้าดูได้

เริ่มเก็บข้อมูลหลังติดตั้ง ไม่มีสถิติย้อนหลังจากก่อนติดตั้ง
ผู้เข้าชมหมายถึงรหัสสุ่มประจำเบราว์เซอร์ ไม่ใช่คนที่ยืนยันตัวตน การใช้หลายเครื่องหรือล้างข้อมูลอาจถูกนับใหม่
จำนวนผู้เข้าชมรวมทั้งช่วงนับรหัสไม่ซ้ำ ไม่ใช่การบวกผู้เข้าชมทุกวันเข้าด้วยกัน
นับเฉพาะ /, /login, /account, /account/orders, /track ไม่รวมหลังบ้าน หน้าพิมพ์ และ API
การเปิดหน้าเดิมซ้ำภายใน 30 วินาทีไม่นับเพิ่ม การกลับมาที่แท็บเดิมหลังจากนั้นนับเป็นการเปิดดูอีกครั้ง
ไม่เก็บชื่อ อีเมล IP หรือพารามิเตอร์ลิงก์ เช่น token ของออเดอร์ ในตารางสถิติ
เบราว์เซอร์ที่ตั้ง Do Not Track หรือไม่อนุญาต localStorage จะไม่ถูกนับ และตัดบอตที่ระบุชัดเจนใน user-agent แต่ไม่สามารถกรองบอตได้ทั้งหมด

ไฟล์ที่เพิ่ม/แก้สำหรับชุดนี้:
app/layout.tsx
app/visit-tracker.tsx
app/api/analytics/route.ts
app/admin/analytics.tsx
app/admin/panel.tsx
lib/analytics.ts
lib/admin-permissions.ts
supabase/05-analytics.sql
tests/analytics.cjs
package.json

ชุด ZIP นี้รวมระบบเลือกบริษัทขนส่งและการแก้ไขก่อนหน้าด้วย ไม่ได้อัปโหลดเข้าเว็บจริงให้อัตโนมัติ
